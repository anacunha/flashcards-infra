name: Deploy

on:
  repository_dispatch:
    types: [deploy-backend]

jobs:
  deploy:
    runs-on: macos-latest

    steps:
      - name: Download S3 file
        run: |
          curl "${{ github.event.client_payload.url }}" -o export-amplify-stack.zip
          ls
