name: Deploy

on:
  repository_dispatch:
    types: [deploy-backend]

jobs:
  deploy:
    runs-on: macos-latest

    steps:
      - name: Checkout 📥
        uses: actions/checkout@v3

      - name: Download S3 file
        run: |
          curl "${{ github.event.client_payload.url }}" -o amplify-export.zip
          unzip export-amplify-stack -d ./lib/
          ls
          ls ./lib/
